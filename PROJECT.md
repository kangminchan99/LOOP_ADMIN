# 프로젝트 정의

## 프로젝트 이름

Loop Admin

## 해결하려는 문제

Loop 모바일 앱과 NestJS 서버가 성장하면서 운영자가 서비스 상태를 한눈에 확인하고, 유저·게시글·댓글·알림·신고를 안전하게 관리할 수 있는 별도 관리자 웹이 필요하다.

이 프로젝트는 운영자가 모바일 앱에 직접 접근하지 않고도 관리 작업을 수행할 수 있게 하는 Next.js 기반 관리자 페이지다.

## 주요 사용자

- Loop 서비스를 운영하는 관리자
- 게시글, 댓글, 알림, 신고 상태를 확인해야 하는 운영자
- 서비스 지표와 사용자 활동을 확인해야 하는 개발자/운영 담당자

## 첫 번째 버전의 핵심 기능

1. 관리자 홈 대시보드에서 주요 운영 지표를 확인한다.
2. 관리자 계정으로 로그인한다.
3. 유저 목록을 조회하고 검색한다.
4. 게시글 목록을 조회하고 검색한다.
5. 댓글 목록을 조회하고 검색한다.
6. 관리자 권한으로 전체 푸시 발송 기능을 호출한다.

## 첫 번째 버전에서 하지 않을 것

- 일반 사용자용 웹 서비스 구현
- 모바일 앱 기능 재구현
- 복잡한 차트/BI 대시보드
- 결제/정산/광고 관리
- 관리자 권한 세분화 RBAC의 완전 구현
- 실시간 모니터링 또는 OpenTelemetry 적용

## 핵심 사용자 흐름

1. 관리자가 `/login`에 접속한다.
2. 관리자 계정으로 로그인한다.
3. 대시보드에서 전체 유저, 게시글, 댓글, 알림 지표를 확인한다.
4. 유저/게시글/댓글 관리 화면으로 이동한다.
5. 검색과 페이지네이션으로 필요한 데이터를 찾는다.
6. 필요한 경우 관리자 기능을 실행한다.

## 성공 조건

- 관리자가 관리자 홈 화면에 접근할 수 있다.
- 관리자 홈 화면이 Next.js Server Component 기반으로 렌더링된다.
- 기본 Next.js 템플릿 화면이 제거되어 있다.
- 환경변수 예시 파일에 NestJS 서버 API 주소가 정의되어 있다.
- 린트와 빌드가 통과한다.
- 관리자 API는 서버에서 `JwtAuthGuard + AdminGuard`로 보호된다.

## 기술적 제약

- 언어: TypeScript
- UI: React
- 프레임워크: Next.js App Router
- 스타일: Tailwind CSS
- 백엔드: 기존 `loop_server` NestJS REST API
- 모바일 앱: 기존 `loop` Flutter 앱
- 배포 후보: Vercel, AWS Amplify, EC2/Nginx, 또는 사내 정적/Node 호스팅

## 데이터와 외부 시스템

- 개인정보 저장 여부: 관리자 화면은 유저 이메일, 닉네임 등 개인정보를 조회할 수 있으므로 접근 통제가 필요하다.
- 외부 API 사용 여부: 직접 외부 API를 호출하지 않고, 기본적으로 NestJS 서버 API만 호출한다.
- 결제정보 처리 여부: 첫 번째 버전에서는 처리하지 않는다.
- AI가 접근하면 안 되는 정보: 실제 운영 토큰, 관리자 계정 비밀번호, 운영 DB 원본, 비공개 고객 데이터.

